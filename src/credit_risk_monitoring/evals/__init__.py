"""Branch-correctness eval — Layer 1 (chain + depth) and Layer 2 (groundedness).

``checks.build_suite`` and ``criteria.build_criteria`` extend the shared
``agent-evals`` framework with this project's domain checks; ``score`` runs them.
"""
